#include <stdio.h>

/**
 * main - entry point
 * @argc: this counts the number of arguments
 * @argv: the pointer to this arguments
 *
 * Return: Alway Success 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	(void)argc;

	return (0);
}
