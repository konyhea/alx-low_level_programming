#include "main.h"

/**
 * mul - fnction that multiplies two integer
 * @a: first digit to be multiplied
 * @b: the scond digit to be multiplied
 * Return: Always Success
 */

int mul(int a, int b)
{
	return (a * b);
}
